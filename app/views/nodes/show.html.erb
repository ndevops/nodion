<h1><%= @node.hostname %></h1>
<p><%= @node.plan.ram %> / <%= @node.plan.cpu %> / <%= @node.plan.storage %> <%= @node.plan.storage_type %> / <%= @node.image.name %></p>
<p>IPv4: <% if @node.ips.any? %><%= @node.ips.first.value %><% end %></p>
<p>Status: <%= @node.status %></p>
<hr>
<p><%= link_to 'Delete', node_path(@node), method: :delete, data: { confirm: 'Are you sure?' }, :class => 'button button-red button-large' %></p>
